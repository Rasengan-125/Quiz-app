## Remaining Bugs

1. Typing an invalid option at the very first login screen exits the entire program instead of looping back and asking again.✅

---

## Improvements Worth Making

2. There is no way to view your own score history as a student — you take the quiz, see the score once, and it's gone unless an admin looks it up.✅
3. Teacher quiz creation doesn't validate the answer field — a teacher can type "Z" as the answer to a multiple choice question that only has options A through D, and it gets saved without complaint. ✅
4. There is no way for a teacher to view or delete quizzes they've already created — only add or replace.✅
5. No confirmation prompt before deleting a student or teacher — one mistyped matric and the record is soft-deleted with no way to undo it from within the app. ✅
